# Paradise Dental Frontend - Production Environment
# Update VITE_API_BASE_URL to your production API endpoint before building

# API Base URL (Update this before running npm run build)
# Option 1: If using subdomain for API
VITE_API_BASE_URL=https://api.yourdomain.com

# Option 2: If API is on same domain with /api path
# VITE_API_BASE_URL=https://yourdomain.com/api

# Note: After changing this file, you must rebuild the frontend:
# npm run build
